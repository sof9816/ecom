import 'package:flutter/material.dart';

const gImagePath = "assets/images/";
var gLocationPermission = false;
bool isUserValid = true;

gScreenWidth(context) {
  return MediaQuery.of(context).size.width;
}
